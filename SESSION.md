# Session Log

## 2026-09-10 — восстановление исходников лендинга WU JI из задеплоенной сборки

### Что сделано

- Разобрана задеплоенная сборка `https://3xjztnoy6wxga.kimi.page`: `index.html`,
  `assets/index-Cc1h_yD9.js` (398 КБ), `assets/index-BzCp-gj3.css` (81 КБ).
- Карт исходников (`.js.map`) нет — вернули 404. Зато в бандле сохранились атрибуты
  `code-path` вида `src/components/Navigation.tsx:25:5`, которые Kimi проставляет
  каждому элементу JSX. Из них собрана карта: 15 файлов, 160 якорей
  «файл → строка → колонка».
- По этой карте восстановлены все исходники: каждый элемент JSX поставлен ровно на
  ту строку и колонку, что в оригинале. Проверка — скриптом-верификатором
  (лежал во временной папке сессии, в репозиторий не попал).
- Определён стек по содержимому бандла: React 19.2.3, GSAP 3.15.0 + ScrollTrigger,
  Lenis, react-router-dom, Tailwind 3 (+ tailwindcss-animate), Vite с `base: './'`.
- Из собранного CSS вынуты кастомные слои: переменные темы `:root`, стили `body`,
  курсор Win98, `.font-display` / `.font-data`, `.phosphor-glow(-white)`,
  `.scanline-overlay`, цвета `phosphor` `#00FF41` / `crt` `#E8E8E8` /
  `gridline` `#1A1A1A`, анимации `blink` и `chevron-pulse`.
- Скачаны оригинальные ассеты: `brain-bg.png`, `logo-head-mouth.png`,
  `cursor-win98.png` (3.5 МБ суммарно).
- Проект собран (`npm run build` проходит), поднят на `localhost:5199` и сверен
  с оригиналом в браузере.
- Создан публичный репозиторий `undermindOS/wuji-project`, запушены `main` и тег `v0.1.0`.

### Текущее состояние

**Работает:**

- `npm run dev` / `npm run build` / `npm run preview` — без ошибок.
- Сверка с оригиналом сошлась по четырём независимым срезам:
  - 160 якорей `code-path` — все на своих местах;
  - DOM: 285 узлов, хеш структуры `e88a42af` — совпал;
  - весь текст страницы, хеш `3bf02fd6` — совпал;
  - computed styles, 20 селекторов × 20 свойств, хеш `91784be9` — совпал
    (после нормализации домена в URL ассетов);
  - интерактив: клик по панели UPPERMIND даёт 214 узлов и хеш `80bef80e` —
    совпал, шапка так же показывает `YOU_HAVE_SELECTED UPPERMIND OS`.

**Расхождения с оригиналом (осознанные):**

- Скрипт `https://www.kimi.com/sdk-seed.js` не перенесён — это скрипт хостинга Kimi
  (он же рисует бейдж «Kimi» в углу), к лендингу отношения не имеет.
- Собранный CSS — 16.7 КБ против 81 КБ у оригинала. В исходном проекте, судя по
  следам в CSS (переменные `--radix-accordion-*`, классы вариантов кнопок shadcn/ui),
  лежал неиспользуемый скаффолд `components/ui/*`. В бандл JS он не попал —
  вырезан tree-shaking, восстанавливать неоткуда. На рендер не влияет: переменные
  темы перенесены полностью.
- Комментарии в коде и имена локальных переменных — реконструкция: минификатор их
  стёр. Структура строк при этом совпадает с оригиналом.

**Не сделано / открыто:**

- Все ссылки — заглушки `https://github.com`, перенесены из оригинала как есть.
  Перечислены таблицей в `README.md`. Настоящие адреса не выбраны.
- Тестов нет. Линтера нет.
- Деплой не настроен.

### Следующие шаги

- Заменить заглушки `https://github.com` на настоящие адреса — по таблице в `README.md`.
- Решить, нужен ли обратно скаффолд shadcn/ui (сейчас его нет, и он не используется).
- Настроить деплой (GitHub Pages / Vercel). Учесть `base: './'` в `vite.config.ts`.

### Ключевые решения

- **Восстанавливать исходники, а не класть статическую копию сборки.** Выбор
  собеседника из трёх вариантов. Даёт редактируемый код ценой риска мелких
  расхождений — риск закрыт сверкой DOM и вычисленных стилей.
- **`code-path` как основа реконструкции, а не чтение минифицированного кода на глаз.**
  Атрибуты дают точные строку и колонку, поэтому форматирование восстановлено
  проверяемо. Показательный случай: якорь `109:131` сошёлся посимвольно и подтвердил,
  что `<h2>` в оригинале записан одной строкой вместе с inline-стилем и текстом.
- **`base: './'` в конфиге Vite восстановлен по косвенному признаку:** в собранном
  CSS путь курсора записан как `../images/cursor-win98.png`, хотя в исходнике он
  абсолютный (`/images/...`). Так Vite переписывает пути из `public/` при
  относительном `base`.
- **Неиспользуемые компоненты shadcn/ui не восстанавливались.** Их нет в бандле,
  восстановить точно нельзя, а придумывать — значит добавлять от себя.
- **Заглушки не помечены `TODO` в коде.** Просьба была «ничего не добавлять»,
  поэтому места-заглушки описаны в `README.md`, а сам код не тронут.
- **Служебные файлы добавлены осознанно:** `README.md`, `CHANGELOG.md`, `.gitignore`,
  `src/vite-env.d.ts` (без последнего `tsc -b` падает на импорте `./index.css`).
